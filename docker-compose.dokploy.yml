services:
  web:
    image: mikecebul/drug-test-mi:branch-test
    pull_policy: always
    restart: always
    ports:
      - 3000
    env_file:
      - .env
    networks:
      - dokploy-network

  worker-redwood:
    image: mikecebul/drug-test-mi:branch-test-worker
    pull_policy: always
    restart: always
    env_file:
      - .env
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
