# Implementation Plans

Generated by the improve skill on 2026-06-17. Execute in the order below unless dependencies say otherwise. Each executor should read the plan fully before starting, honor its STOP conditions, and update the row when done.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Require Cal.com webhook signatures outside local development | P1 | S | - | DONE |
| 002 | Add route-level tests for Cal.com webhook ingestion | P1 | M | 001 | DONE |
| 003 | Recover from duplicate Cal.com webhook create races | P1 | M | 002 | DONE |
| 004 | Centralize Cal.com booking config generation | P2 | M | - | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Notes

- 002 depends on 001 because route tests should assert the hardened missing-secret behavior.
- 003 depends on 002 because duplicate-delivery handling needs route-level tests before changing webhook upsert behavior.
- 004 can run independently because it touches dashboard/config generation rather than webhook ingestion.

## Verification Baseline

Recon on 2026-06-17:

- `pnpm exec tsc --noEmit --pretty false` exited 0.
- `pnpm exec vitest run src/app/'(payload)'/api/webhooks/calcom/__tests__/calcomWebhook.test.ts src/utilities/__tests__/calcom-config.test.ts src/views/DrugTestWizard/workflows/complete-workflow/schedule-utils.test.ts` exited 0 with 3 files and 9 tests passing.
- `pnpm audit --prod --audit-level high` exited 1 and reported high advisories through `mongoose` and `ws`. This is tracked as a rejected/deferred finding below, not part of these Cal.com plans.

## Findings Considered And Rejected

- Production dependency audit has high advisories in transitive Payload/Mongoose/ws paths: deferred because the user selected the Cal.com integration recommendation set, and dependency remediation has a different blast radius than webhook/config hardening.
- Admin guided workflow UI polish: deferred because the stronger immediate leverage is webhook correctness, route test coverage, and config de-duplication. The current guided workflow uses existing shadcn-style `Card`, `Badge`, `Button`, `Input`, `RadioGroup`, and Sonner patterns.
