# Changesets

This directory contains release metadata generated by Changesets.

- Run `pnpm changeset` in a feature branch to create a changeset file.
- Merge PRs with changesets into `main`.
- The `Version Packages` workflow opens/updates an automated version PR.
