export {
  BaseVerifyDataFieldGroup as VerifyDataFieldGroup,
  verifyDataFieldSchema,
} from '../../field-groups/BaseVerifyDataFieldGroup'

/**
 * Instant Test workflow-specific wrapper for VerifyDataFieldGroup
 * Uses BaseVerifyDataFieldGroup with workflow-specific configuration
 */
