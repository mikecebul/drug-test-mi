import { Block } from 'payload'

export const SchedulePage: Block = {
  slug: 'schedulePage',
  interfaceName: 'SchedulePageBlock',
  fields: [
    {
      name: 'title',
      type: 'text',
    },
  ],
}
