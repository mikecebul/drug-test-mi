/**
 * Type escape used only by migrations that ran before test types moved to
 * static configuration. The collection is intentionally not registered in
 * the current Payload config.
 */
export const LEGACY_TEST_TYPES_COLLECTION = 'test-types' as never
